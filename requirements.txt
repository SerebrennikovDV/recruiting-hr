# Зависимости веб-сервиса «UnitHire» с замороженными версиями.
# Установка в чистое виртуальное окружение:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Версии зафиксированы (pip freeze) для воспроизводимости сборки.

Django==4.2.20            # веб-фреймворк (LTS, поддерживает Python 3.9+)
psycopg2-binary==2.9.10   # драйвер PostgreSQL (промышленная СУБД)
dj-database-url==2.3.0    # конфигурация БД через переменную окружения DATABASE_URL
python-dotenv==1.0.1      # загрузка переменных окружения из файла .env
Pillow==10.4.0            # обработка изображений (валидация загружаемых файлов)
django-filter==23.5       # фильтрация списков (вакансии, кандидаты)
python-docx==1.1.2        # формирование документов .docx (карточка кандидата)
openpyxl==3.1.5           # формирование документов .xlsx (аналитические отчёты)
matplotlib==3.9.4         # серверная отрисовка графиков HR-аналитики
gunicorn==23.0.0          # WSGI-сервер приложений для продакшена
whitenoise==6.8.2         # раздача статических файлов в продакшене
requests==2.34.2          # обращение к программным интерфейсам площадок подбора
pymorphy3==2.0.6          # морфологический разбор русского текста при отборе резюме
